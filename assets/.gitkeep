# SVG texture tiles only. The 9-slice border-image pipeline was descoped
# by Evening 0 (docs/decisions.md E1): GTK does not clip border-image to
# border-radius, so 9-slice bevels draw square corners over rounded fills.
# Raster, if ever, always paired through -gtk-scaled().
